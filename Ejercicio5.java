import java.util.Scanner;
public class Ejercicio5 {
  public static void main(String[]args) {
    //definir variables
    Scanner leer=new Scanner(System.in);
    int i;
    double Costo;
    //Datos de entrada
    System.out.print("Pagara: ");
    i=leer.nextInt();
    if (i>=200);
    Costo=i+i*0.15;
    if (i<200);
    Costo=i+i*0.12;
    if (i<=100);
    Costo=i+i*0.1;
    System.out.println("Pagara :"+Costo);
    }
  }